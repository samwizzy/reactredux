import NotFound from './NotFound'

const notFoundConfig = [
    {
        routes: [
            {
                component: NotFound
            }
        ]
    }
];

export default notFoundConfig;